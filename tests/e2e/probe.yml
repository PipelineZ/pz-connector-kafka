connection:
  # Interpolated from the environment by `pz connector test --config`, the same way connections.yml
  # is: the CI job exports KAFKA_BOOTSTRAP for both.
  bootstrap_servers: ${KAFKA_BOOTSTRAP}
read:
  dataset: e2e-in
write:
  output: e2e-probe
  mode: append
